# Trigger顶级必要的配置参数
Desc = "使用Http监听端口并接收POST原始字节数据"
Name = "HttpServerTrigger"
Topic = "http/001"

# HttpServer配置参数
[HttpServerOptions]
  listenAddress = "0.0.0.0:5571"
  readTimeout = "10s"
  writeTimeout = "10s"
